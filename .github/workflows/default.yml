name: Build Check
run-name: Running Java build
on:
  - push
  - pull_request

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21 for x64
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          architecture: x64
          cache: 'maven'
  checks:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - run: mvn spotless:check

  build:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - run: mvn compile
